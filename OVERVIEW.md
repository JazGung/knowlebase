---
title: 知识库构建与检索系统 - 功能总览
---

## 1. 文档管理

管理企业文档的上传、查询、状态变更与清理。

### 1.1 列表
分页查询文档列表，支持按状态（pending/processing/success/failed）、启用状态、分类过滤及关键字搜索。默认按创建时间降序排列。

### 1.2 上传
支持批量文件选择和并发上传，前端计算文件MD5哈希并本地去重，后端提供重复性校验接口和单文件上传API。文件格式验证（仅允许PDF、Word），单文件最大100MB。上传文件保存到MinIO对象存储，数据库创建元数据记录并自动触发异步处理任务。

### 1.3 处理
对已有文档重新触发完整的处理流水线（解析→清洗→分块→入库）。包括上传时的自动触发和列表页的文档处理操作（容错封装）。

### 1.4 启用
切换文档的启用状态。启用的文档可参与知识库版本重建，停用的文档不参与。

### 1.5 停用
切换文档的停用状态。

### 1.6 扫描
扫描 MinIO 存储桶识别无数据库记录的孤立文件，以弹出框展示扫描结果，用户勾选后确认清理。

### 1.7 清理日志
提供清理操作日志的分页查询，支持按日期范围、清理原因、执行者过滤。

## 2. 知识库管理

管理知识库版本的创建、启用、停用与回退。

### 2.1 新建
手动触发知识库重建，创建新版本记录（状态初始为building）。系统检查是否存在building中的版本（重建锁），不存在才允许创建。新版本遍历当前所有已启用文档，执行完整处理流水线（解析→清洗→分块→入库），完成后状态标记为succeeded。
