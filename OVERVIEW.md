---
title: 知识库构建与检索系统 - 功能总览
---

## 功能概览

文档管理
  |_文档列表
  |_文档上传
  |_文档启用
  |_文档停用
  |_文档扫描
  |_文档清理日志
知识库管理
  |_版本列表
  |_版本新建
  |_版本构建
  |_版本启用
文档处理管理
  |_文档-版本关联查询
  |_处理记录查询
  |_处理详情查询

## 1. 文档管理

管理企业文档的上传、查询、状态变更与清理。

### 1.1 文档列表
分页查询文档列表，支持按状态（待处理/处理中/成功/失败）、启用状态、分类过滤及关键字搜索。默认按创建时间降序排列。

### 1.2 文档上传
支持批量文件选择和并发上传，前端计算文件MD5哈希并本地去重，后端提供重复性校验接口和单文件上传API。文件格式验证（仅允许PDF、Word），单文件最大100MB。上传文件保存到MinIO对象存储，数据库创建元数据记录并自动触发异步处理任务。

### 1.3 文档启用
切换文档的启用状态。启用的文档可参与知识库版本构建，停用的文档不参与。

### 1.4 文档停用
切换文档的停用状态。

### 1.5 文档扫描
扫描 MinIO 存储桶识别无数据库记录的孤立文件，以弹出框展示扫描结果，用户勾选后确认清理。

### 1.6 文档清理日志
提供清理操作日志的分页查询，支持按日期范围、清理原因、执行者过滤。

## 2. 知识库管理

管理知识库版本的创建、启用与查询。系统始终保持有且仅有一个启用版本。

### 2.1 版本列表
分页查询版本列表，支持按状态过滤，按版本名称降序排列。

### 2.2 版本新建
创建新版本记录，状态初始化为 初始化。

### 2.3 版本构建
手动触发知识库构建，遍历当前所有已启用文档执行完整处理流水线（解析→清洗→分块→入库）。仅可对 初始化 或 失败 状态的版本发起构建，构建期间阻塞文档的上传/启用/停用操作。完成后状态标记为 成功，失败标记为 失败。

### 2.4 版本启用
启用指定版本，自动替换当前启用版本（将原启用版本改为 已禁用，目标版本改为 已启用）。仅可启用 status 为 成功 或 已禁用 的版本。

## 3. 文档处理管理

管理文档处理流水线的执行与查询，作为文档管理与知识库管理之间的桥梁领域。

### 3.1 文档-版本关联查询
按文档或版本维度查询 document_version_relation 列表，作为处理记录的入口视图。支持按版本过滤（知识库管理入口）和按文档过滤（文档管理入口）。

### 3.2 处理记录查询
查询指定关联记录的处理历史（document_processing_history），支持按 attempt_no 降序分页，展示处理状态、开始时间、完成时间和错误信息。

### 3.3 处理详情查询
查询指定处理记录的处理阶段详情（processing_stage_result），展示各阶段（检查→解析→清洗→图片描述→分块→入库）的运行状态、耗时和中间结果。
